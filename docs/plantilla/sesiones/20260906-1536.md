# Sesión 2026-09-06 15:36 — guía revisada, Pages de la plantilla y Cargo.lock

1. **Qué cambió:** (a) `init-plantilla.yml` reordena `Cargo.lock` tras el renombrado —cargo ya no genera un diff espurio en el primer build— y sustituye en `docs/*.html` en vez de en dos nombres fijos. (b) Descubierto que **el sitio de la plantilla sirvió el README desde siempre**: el Source de Pages seguía en «Deploy from a branch» y los runs verdes de `pages.yml` no lo detectan; el usuario lo cambió y ahora sirve `docs/`. La regla de verificación de `CLAUDE.md` se endurece con la señal que lo delata (un run `pages build and deployment` con `Build with Jekyll`), y `ARRANQUE.md` y `guia.html` lo recogen. (c) Nueva **`docs/guiaFable.html`**, revisión a fondo de la guía conservando `guia.html` para comparar: análisis de cuándo este flujo es la opción más eficiente y cuándo no, el prompt de arranque en dos bloques cortos (el método vive en `CLAUDE.md`; no se duplica en el prompt), tabla de qué vale como verificación, y errores ampliados. En `PruebaInit` (desechable, no portado) el backend de Fly sirve el mock.
2. **SHA:** `fb8dc1d` en `main`; runs [34042833996](https://github.com/npiobject/DesdeMovil/actions/runs/34042833996) `pages.yml` **success** (sin constructor de Jekyll detrás: Source ya en GitHub Actions) y `init-plantilla.yml` **skipped**. Anteriores de la misma tanda: `7c0c892` (Cargo.lock), `c08da66` (guía anterior al día).
3. **URLs para probar:** https://npiobject.github.io/DesdeMovil/guiaFable.html (build `DM-B3-20260906-005`) · https://npiobject.github.io/DesdeMovil/guia.html (`-004`) · https://npiobject.github.io/DesdeMovil/ (el mock, por fin en la raíz). Verificadas por el run; la página la abrió el usuario.
4. **Drive:** este resumen en `sesiones/` y copia de `guiaFable.html` en la raíz de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`), sin borrar nada.
5. **Qué falta:** decidir, comparando las dos guías, cuál queda como oficial y borrar la otra; e integrar el bloque «El prompt de arranque» de `guiaFable.html` en el proyecto generador de prompts —**[SUPUESTO]** que ese proyecto produce el prompt del *qué* y este bloque aporta el *cómo*; plan B si genera también reglas de método: recortarlas, porque ya están en `CLAUDE.md`. Y lo de siempre: `tools/*.ps1` en un PC con PowerShell.
