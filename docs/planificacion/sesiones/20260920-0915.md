# Sesión 2026-09-20 09:15 — F1: CRUD de notas en SQLite

1. **Qué cambió**: `app/` con `rusqlite` (tablas `notas` y `etiquetas`), rutas `POST/GET /notas`, `GET/DELETE /notas/{id}`, `GET /etiquetas` con filtros `q`, `etiqueta`, `desde`, `hasta` y título de respaldo (D6); volumen `datos` en Fly (`fly.toml` + `deploy.yml`, que además verifica `/notas`); `docs/index.html` es el mock 2 contra la API (mock 1 archivado en `docs/mocks/002-notas-local.html`); plan v2 (D3 confirmado, F0 y F1 hechas) y `CLAUDE.md` con las rutas.
2. **SHA**: `cfb3818` en `main` (merge del PR #2). Runs: `deploy.yml` 35495661217 y `pages.yml` 35495661204, ambos `success`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (build `PR-B1-20260920-004`) · backend https://prueba-npiobject-labs.fly.dev/notas · salud https://prueba-npiobject-labs.fly.dev/salud
4. **Drive**: `plan-notas-v2.md` y copia de este resumen en `sesiones/`.
5. **Qué falta**: F2 (LLM: título y etiquetas reales; necesito URL, formato y nombre del secreto de la API de agentes), F3 (FTS5, acentos), F4 (PWA + token D8). Riesgo abierto: la API es pública sin token hasta F4.
