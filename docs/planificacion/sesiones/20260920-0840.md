# Sesión 2026-09-20 08:40 — plan y mock 1 de la app de notas

1. **Qué cambió**: `docs/planificacion/plan-notas.md` (requisitos, D1-D10, modelo, API, fases F0-F4); `docs/index.html` pasa a ser el mock 1 de notas (localStorage, dictado, título/etiquetas simulados, búsqueda por texto/etiqueta/fecha); mock 0 archivado en `docs/mocks/001-inicio.html`.
2. **SHA**: `f706e25` en la rama `claude/gallant-heisenberg-lf8dpg`, PR de borrador [#1](https://github.com/npiobject-labs/prueba/pull/1) hacia `main`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (build `PR-B1-20260920-003`) **solo cuando se fusione el PR en `main`**; Pages no publica ramas de trabajo.
4. **Drive**: subido `plan-notas.md` a la carpeta `prueba` y copia de este resumen en `sesiones/`.
5. **Qué falta**: datos de la API LLM (URL, formato, secreto); confirmar D3 y D8; validar el mock desde el móvil; fusionar el PR y comprobar el run de `pages.yml`; después F1 (backend CRUD).
