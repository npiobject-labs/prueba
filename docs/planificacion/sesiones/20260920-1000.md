# Sesión 2026-09-20 10:00 — F3 (búsqueda FTS5) y F4 (PWA + token)

1. **Qué cambió**: `app/`: tabla FTS5 `notas_fts` sin acentos y por prefijo, con triggers y reindexado al arrancar; token opcional `TOKEN_API` (401 sin él en `/notas*` y `/etiquetas`), `/salud` indica si está activo. `deploy.yml`: fija `TOKEN_API` desde el secreto de repositorio y verifica 401 sin token y búsqueda `reunion`→«Reunión». `docs/`: mock 5 (`PR-B1-20260920-007`) con ⚙ Ajustes (token, instalar), banda sin conexión, `manifest.webmanifest`, iconos y `sw.js`. Mock 4 archivado en `docs/mocks/005-notas-reintento.html`. Plan v3 y `CLAUDE.md` al día.
2. **SHA**: merge del PR #7 en `main` (ver bitácora).
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ · instalable desde el navegador.
4. **Drive**: `plan-notas-v3.md` y copia de este resumen en `sesiones/`.
5. **Qué falta**: el usuario crea el secreto de repositorio `TOKEN_API` y lo pega en Ajustes (hasta entonces la API sigue abierta); F2 (LLM) aparcada a falta de los datos de la API.
