# Sesión 2026-09-24 10:28 — subcarpetas en proyectos, del mock al despliegue

1. **Qué cambió**: subcarpetas dentro de los proyectos (F23 y F24, D43 a D53), con las respuestas del usuario al mock 19: un nivel, el padre enseña también lo de sus subcarpetas, borrado sin perder nada y «subcarpeta» en la interfaz. Backend: migración 3 (`padre_id`), `padre` en `/proyectos`, filtros `proyecto=` que incluyen subcarpetas y `solo=1`, archivar y borrar según D51/D52, `proyecto.padre` en notas, documentos y entrevistas. App (mock 19): tira Todo · Sin subcarpeta · 📂 · ＋ Subcarpeta, migas «Padre ›», chips 📂, «Dentro de» y lista de subcarpetas en la hoja del proyecto, árbol en «Mover a…», rutas en el editor y exportación por ruta. `deploy.yml` gana el paso «Verificar subcarpetas».
2. **SHA**: `732a282` (merge del PR #47). Runs en verde: Fly `35975468214` (con «Verificar subcarpetas» contra el backend real) y Pages `35975468255`. Build `PR-B1-20260924-026`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (si no sale la tira de subcarpetas, ⚙ Ajustes → Buscar actualización) · https://prueba-npiobject-labs.fly.dev/salud
4. **Drive**: copia de `plan-subproyectos.md` (v2) y de este resumen en `sesiones/`.
5. **Qué falta**: probarlo en el móvil con las notas reales. El plan no deja nada pendiente; si hiciera falta un segundo nivel, el plan B de D43 solo exige quitar el tope en la API y sangrar el árbol.
