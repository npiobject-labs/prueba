# Sesión 2026-09-07 08:10 — el residuo que rompía la inicialización

1. **Qué cambió:** revisión de si el proyecto está terminado. Encontrado un bloqueante real introducido por `637805f`: `docs/guiaNew.html` traía `guia-desdemovil-v2` como clave de `localStorage`, el sed de `init-plantilla.yml` solo sustituye `DesdeMovil` y `npiobject` en `docs/*.html`, y el paso antirresiduos hace `grep -i desdemovil` — así que **cualquier repo creado hoy desde la plantilla habría fallado el run**. Invisible aquí porque en la plantilla el workflow sale `skipped`. Arreglado el residuo y endurecido el workflow: sustituye también la minúscula, y aparta/restaura el enlace a la plantilla de origen (`github.com/npiobject/DesdeMovil` y el `template_name`/`template_owner`) en `docs/*.html`, no solo en `ARRANQUE.md` —hasta ahora las guías heredadas quedaban en el hijo apuntando a un repo inexistente—, con el `grep` excluyendo las dos formas. De paso, fuera los enlaces a `PruebaPlantilla` (desechable) y corregida la frase de `guiaNew.html` que daba los `tools/*.ps1` por probados: no lo están. Cerrados los PR [#2](https://github.com/npiobject/DesdeMovil/pull/2) y [#3](https://github.com/npiobject/DesdeMovil/pull/3), superados por el portado a mano de `63c590a`.
2. **SHA:** el arreglo entró en `main` como `6e54fb7` (PR #4, rebase) y el catálogo de guías como `7c0426c` (PR #5). Runs de `pages.yml` [34099287361](https://github.com/npiobject/DesdeMovil/actions/runs/34099287361) y [34099916443](https://github.com/npiobject/DesdeMovil/actions/runs/34099916443), los dos **success**, con `Inicializar plantilla` en `skipped` y sin constructor de Jekyll detrás.
3. **URLs para probar:** https://npiobject.github.io/DesdeMovil/guiaNew.html (`DM-B3-20260907-006`), https://npiobject.github.io/DesdeMovil/guiaFableMini.html (`-007`) y https://npiobject.github.io/DesdeMovil/guiaFable.html (`-005`); el mock sigue en https://npiobject.github.io/DesdeMovil/ (`DM-B3-20260905-001`). La verificación de fondo no es Pages: es el paso antirresiduos, simulado sobre un árbol limpio con `pepe/CasaVerde`, `otro/proyecto_2` y `npi/DM2`, verde en los tres.
4. **Drive:** copia de este resumen en `sesiones/` de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`), sin borrar nada.
5. **Qué falta:** las guías quedaron resueltas en la addenda. Lo que no se puede cerrar desde una sesión: **la prueba real del arreglo**, que es crear un repo nuevo desde la plantilla y ver `init-plantilla.yml` en verde (aquí sale siempre `skipped`, y `PruebaInit` se validó con `aed8a4e`, anterior a las guías). Y el de siempre: **[SUPUESTO]** los `tools/*.ps1` nunca se han ejecutado; plan B, pasar `-Root` explícito. `PruebaInit` es desechable cuando quieras.

## Addenda 09:00 — catálogo de guías

Decidido: se borra `docs/guia.html` (su índice es un subconjunto estricto de `guiaFable.html`, que además añade cuatro secciones) y **se conservan las tres restantes como enfoques distintos**, no como versiones de lo mismo:

| Guía | Rol | Forma | Build |
|---|---|---|---|
| `guiaFableMini.html` | arrancar con el móvil en la mano | checklist de 3 pasos, campo de nombre que rellena enlaces y prompt, botones de copiar, progreso | `DM-B3-20260907-007` |
| `guiaNew.html` | el recorrido entero, paso a paso | manual operativo: 19 casillas, cuentas conectadas, diagnóstico, cómo borrar un proyecto sin restos | `DM-B3-20260907-006` |
| `guiaFable.html` | entender el método y decidir si compensa | prosa y 5 tablas: alternativas, prompt de arranque, qué vale como verificación | `DM-B3-20260906-005` |

De paso: `guiaNew` y `guiaFableMini` venían como **fragmentos de artifact**, sin `<!doctype>`, `<head>`, `charset` ni `viewport` — es decir, servidas por Pages se renderizaban a 980 px en el móvil, que es justo su público. Envueltas en HTML completo y con su `<meta name="build">`, que exige `CLAUDE.md`. `README.md` y `ARRANQUE.md` llevan ahora la tabla de las tres, y los enlaces cruzados entre ellas quedan cerrados.
