# Sesión 2026-09-06 14:45 — prueba definitiva de la inicialización

1. **Qué cambió:** nada de código; se cerró la verificación. Borrado y recreado `npiobject/PruebaInit` desde la plantilla ya corregida y, esta vez, **sin tocar nada**: `init-plantilla.yml` terminó en `success` en su `run_number: 1` por push del *Initial commit*, 13 s después de crear el repo, y su commit no toca ningún fichero de `.github/workflows/` —la causa del fallo de la ronda anterior—. Activado Pages y relanzado `pages.yml`: verde, sitio publicado. Cerrado el `[SUPUESTO]` de `prueba-init.md` y añadida la tabla de estado final.
2. **SHA:** `aed8a4e` en `DesdeMovil`/`main` cerró el supuesto; este resumen va encima. En `PruebaInit`: inicialización en `3049bae` (run [34039428339](https://github.com/npiobject/PruebaInit/actions/runs/34039428339)) y Pages en el run [34039653290](https://github.com/npiobject/PruebaInit/actions/runs/34039653290), los dos **success**.
3. **URLs para probar:** https://npiobject.github.io/PruebaInit/ (build `PI-B1-20260905-001`) · https://npiobject.github.io/DesdeMovil/. En el árbol: `pruebainit-backend`, usuario `pruebainit`, `GET /` → `PruebaInit backend`, **Parámetros** rellenada con Drive vacío, `docs/plantilla/` con los 8 documentos, `docs/planificacion/` limpia, marcador borrado, ningún `# PLANTILLA` y el workflow en `disabled_manually`.
4. **Drive:** `prueba-init.md` actualizado (versión `-v2`, sin borrar la anterior) y este resumen en `sesiones/` de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`).
5. **Qué falta:** nada de la inicialización: los dos pasos manuales que promete `ARRANQUE.md` son exactamente dos y el resto sale solo. Pendientes de siempre: probar `tools/*.ps1` en un PC con PowerShell, y `PruebaInit` es desechable cuando quieras.
