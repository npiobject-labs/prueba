# Sesión 20260909-1934

1. **Qué cambió:** el repo pasa a `npiobject-labs/DesdeMovil` y `FLY_API_TOKEN` deja de ser un secreto por proyecto: es de la organización y lo heredan sus repos **públicos**. Puestos al día `ARRANQUE.md` (enlace *Use this template* con `template_owner=npiobject-labs`, aviso de elegir Owner y repo público, fase de Fly sin creación de token ni secreto de repo, URLs de Pages y de `aterrizar.ps1`), `CLAUDE.md` (owner, fuente de verdad, Pages, API de Actions, Despliegue) y las tres guías de `docs/` (fase de Fly, averías y borrado de proyecto: ya no se revoca ningún token). `tools/aterrizar.ps1` con `-Owner npiobject-labs`. En `init-plantilla.yml`, que sí deriva el owner de `GITHUB_REPOSITORY`, dos arreglos: la sustitución del owner pasa por un marcador (`s/npiobject/${OWNER}/g` dejaba `npiobject-labs-labs` cuando el owner es la propia organización) y el paso antirresiduos deja pasar la organización de Fly `desdemovil` de `deploy.yml` y `vigilancia-fly.yml`, que si no **habría hecho fallar la inicialización de todo repo hijo** creado desde hoy. `deploy.yml` sin cambios.
2. **SHA:** `9fd6885` en `claude/determined-newton-m6v7lm`, PR [#7](https://github.com/npiobject-labs/DesdeMovil/pull/7) en borrador. **Sin run verde todavía**: ningún workflow dispara sobre una rama de trabajo (`pages.yml` e `init-plantilla.yml` solo en `main`, `deploy.yml` solo con cambios en `app/**`). Verificado en su lugar: los cuatro pasos reales de `init-plantilla.yml` extraídos del YAML y ejecutados sobre copias del árbol con `pepe/CasaVerde`, `npiobject-labs/Proyecto2` y `otro-labs/DM2` —los tres en verde en el paso antirresiduos y con el owner correcto—, los cuatro workflows pasando `yaml.safe_load` y las cuatro páginas de `docs/` cerrando etiquetas.
3. **URLs para probar:** https://npiobject-labs.github.io/DesdeMovil/guiaNew.html (`DM-B3-20260909-008`), https://npiobject-labs.github.io/DesdeMovil/guiaFableMini.html (`-009`) y https://npiobject-labs.github.io/DesdeMovil/guiaFable.html (`-010`), **cuando el PR esté fusionado**: Pages solo publica desde `main`.
4. **Drive:** copia de este resumen en `sesiones/` de la carpeta configurada en **Parámetros**.
5. **Qué falta:** fusionar el PR #7 y confirmar el run de `pages.yml` en verde sobre `main`. Dos cosas para decidir, ninguna tocada aquí: con el token heredado, `deploy.yml` **despliega ya en el commit inicial** de cada repo nuevo (el commit de plantilla toca `app/**`), así que Fly deja de ser opcional de hecho y cada proyecto nuevo crea su app; y el nombre derivado de la app de este repo pasa a `desdemovil-npiobject-labs`, con `desdemovil-npiobject` quedando huérfana en Fly. `CLAUDE.md` sigue diciendo que `tools/estado.ps1` acepta `-Owner` y `-Remote`, y no los tiene.
