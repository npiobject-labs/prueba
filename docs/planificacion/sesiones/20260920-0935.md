# Sesión 2026-09-20 09:35 — «No funciona Guardar»: arranque en frío de Fly

1. **Qué cambió**: diagnóstico desde Actions (`diagnostico-fly.yml`, PR #5): el backend responde y hay notas guardadas; el fallo era el arranque en frío tras el autoapagado (el proxy de Fly tardó más de los 30 s del navegador). `app/fly.toml`: `auto_stop_machines = "suspend"` (reanuda en milisegundos). `docs/index.html` (mock 4): espera de 45 s, un reintento automático con aviso «arrancando el backend…», botón «Reintentar» en la lista y aviso claro al guardar (el texto no se pierde). Mock 3 archivado en `docs/mocks/004-notas-borrado.html`.
2. **SHA**: merge del PR #6 en `main` (ver bitácora).
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (build `PR-B1-20260920-006`).
4. **Drive**: copia de este resumen en `sesiones/`.
5. **Qué falta**: [SUPUESTO] Fly puede suspender máquinas con volumen; si no, sigue parándolas y el reintento del cliente cubre el arranque. F2 aparcada; F3 y F4 pendientes.
