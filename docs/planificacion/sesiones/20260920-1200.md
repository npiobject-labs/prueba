# Sesión 2026-09-20 12:00 — F2: el título de la nota lo pone el servicio `openrouter`

1. **Qué cambió**: F2 deja de estar aparcada. `app/src/llm.rs` pide título y etiquetas al servicio propio `npiobject-labs/openrouter` (contrato de OpenAI, VPS por defecto o Fly con `LLM_URL`), con salida estructurada `{titulo, etiquetas[]}`, las 40 etiquetas en uso como vocabulario y `X-Operacion: notas-titulo` para medir el gasto. La nota se guarda **antes** de llamar (respaldo D6) y hay `POST /notas/{id}/reintentar-ia`; `/salud` añade `"ia"`. Mock 6 marca «título provisional» y ofrece reintentar; ⚙ Ajustes dice si hay IA. `deploy.yml` vuelca `LLM_CLAVE`/`LLM_URL`/`LLM_MODELO` y verifica el titulado. Guía nueva: `docs/planificacion/titulado-ia.md`.
2. **SHA**: rama `claude/zealous-wright-wzqltl`, PR #9 en `npiobject-labs/prueba`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (build `PR-B1-20260920-008`) en cuanto el PR entre en `main`.
4. **Drive**: copia de este resumen en `sesiones/` (pendiente del cierre con conector).
5. **Qué falta**: dar de alta la aplicación `notas` en el servicio (`tools\apps.ps1 -Crear notas`), ponerle presupuesto y guardar la clave como secreto `LLM_CLAVE` aquí; hasta entonces las notas llevan título provisional. Sigue pendiente `TOKEN_API`.
