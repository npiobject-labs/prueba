# Sesión 2026-09-23 10:30 — notas por proyectos, implementado y desplegado

1. **Qué cambió**: el plan de la entrada de las 10:10, hecho entero salvo la fase opcional. Backend: tabla `proyectos` con migración por `user_version`, cinco rutas de proyectos, filtro por proyecto en notas, etiquetas y documentos, mover notas de una en una y en bloque, y el proyecto como contexto para la IA al redactar documentos. App (mock 13): pantalla de Proyectos con búsqueda, orden, crear, editar, archivar y borrar; la app se abre en el último proyecto; «Nueva nota» cae dentro; «En todos los proyectos» en el buscador; «Mover» en la selección y «Mover a…» en el detalle; documentos filtrados por proyecto; exportación agrupada y por proyecto. `deploy.yml` verifica proyectos en cada despliegue.
2. **SHA**: ver bitácora de esta sesión (commit de merge del PR #26 en `main`). Build `PR-B1-20260923-015`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ · backend https://prueba-npiobject-labs.fly.dev/salud
4. **Drive**: copia de este resumen en `sesiones/`, del plan como `plan-proyectos-v2.md` y del plan de notas como `plan-notas-v14.md`.
5. **Qué falta**: probarlo en el móvil (pulsar «Actualizar» si sale la banda de versión nueva), repartir las notas de «Sin proyecto», y decidir si se quiere F17 (la IA propone proyecto).

## Primera vez tras el despliegue

La app abre en **Proyectos** con solo «Sin proyecto · N»: ahí están todas las
notas de antes. Crear el primer proyecto con +, volver con 📁, entrar en «Sin
proyecto», ☑ para seleccionar, marcar y **Mover**. No hay asistente y no hace falta.

## Verificación en la sesión

- **Migración**: base creada con el binario de `main` y el nuevo arrancado
  encima; las notas quedan en «Sin proyecto» y un segundo arranque no migra otra vez.
- **Backend**: 409 por nombre repetido sin distinguir mayúsculas ni acentos, 400
  por proyecto inexistente, mover atómico (una nota inexistente anula todo),
  archivar, y borrar un proyecto dejando sus notas vivas. El documento de notas
  de un mismo proyecto lleva el proyecto al modelo; el de notas mezcladas, no.
- **App**: 37 comprobaciones con Chromium a 390×844 contra el backend local y una
  IA simulada, sin errores de consola. El paso nuevo de `deploy.yml` se ejecutó
  también contra el backend local con `pipefail`, como en Actions.
