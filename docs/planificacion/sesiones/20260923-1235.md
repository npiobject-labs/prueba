# Sesión 2026-09-23 12:35 — botón Copiar en la nota

1. **Qué cambió**: en el detalle de una nota, «📋 Copiar» junto a «← Volver»: pone en el portapapeles, en markdown, título, proyecto, fecha, etiquetas y contenido. Respaldo con `execCommand('copy')` y aviso si el navegador no deja. Mock 16.
2. **SHA**: `ca6fe16` (merge del PR #32). Run de Pages `35849746116` en verde. Build `PR-B1-20260923-018`.
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (pulsar Actualizar si sale la banda).
4. **Drive**: copia de este resumen en `sesiones/`.
5. **Qué falta**: probarlo en el móvil; más adelante, que la propia nota ofrezca el resumen (el usuario lo aplazó). La nota «Nueva sección de entrevista» describe una función nueva (grabar diálogo, transcribir, tercer paso) aún sin planificar.
