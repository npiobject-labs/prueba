# Sesión 2026-09-20 09:20 — botón de borrar con confirmación

1. **Qué cambió**: `docs/index.html` (mock 3): papelera en cada tarjeta de la lista y en el detalle, con hoja de confirmación propia (título y fecha de la nota, Cancelar/Borrar, Escape o toque fuera cierran, reintento si falla la API); sustituye al `confirm()` nativo. Mock 2 archivado en `docs/mocks/003-notas-api.html`.
2. **SHA**: ver merge del PR #4 en `main` (en la bitácora).
3. **URL para probar**: https://npiobject-labs.github.io/prueba/ (build `PR-B1-20260920-005`).
4. **Drive**: copia de este resumen en `sesiones/`; el plan no cambia.
5. **Qué falta**: F2 aparcada a petición del usuario (LLM). F3 (FTS5) y F4 (PWA + token). La API sigue pública sin token.
